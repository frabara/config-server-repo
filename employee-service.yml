server:
  port: 8081
spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/employee_db'
    username: root
    password: hola
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  boot:
    message: Hello from employee-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: false
